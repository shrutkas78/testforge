# TestForge default configuration
# Override any key with -Dkey=value or an env var (BASE_URL, BROWSER, HEADLESS)
base.url=https://www.saucedemo.com
browser=chrome
headless=false
explicit.wait.seconds=10
